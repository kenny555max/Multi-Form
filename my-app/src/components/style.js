import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
    GridContainer: {
        display: "flex",
        justifyContent: "center",
    },
    GridItem: {
        textAlign: "center"
    }
}));